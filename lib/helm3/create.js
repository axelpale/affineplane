module.exports = (a, b, x, y, z) => {
  // affineplane.helm3.create(a, b, x, y, z)
  //
  // Create a new helm3 object.
  //
  // Return
  //   a helm3
  //
  return {
    a: a,
    b: b,
    x: x,
    y: y,
    z: z
  }
}
