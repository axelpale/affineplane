module.exports = (p) => {
  // affineplane.point2.toArray(p)
  //
  // Get the point2 object as an array.
  //
  // Parameters:
  //   p
  //     a point2
  //
  // Return
  //   an array [x, y]
  //
  return [p.x, p.y]
}
