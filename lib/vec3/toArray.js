module.exports = (v) => {
  // affineplane.vec3.toArray(v)
  //
  // Convert vector to array with elements [x, y, z]
  //
  // Parameters:
  //   v
  //     a vec3
  //
  // Return
  //   an array
  //
  return [v.x, v.y, v.z]
}
