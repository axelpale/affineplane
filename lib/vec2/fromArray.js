module.exports = (arrp) => {
  // affineplane.vec2.fromArray(arrp)
  //
  // Create { x, y } vector from array [x, y]
  //
  // Return
  //   a vec2
  //
  return {
    x: arrp[0],
    y: arrp[1]
  }
}
