module.exports = (v) => {
  // affineplane.vec2.toArray(v)
  //
  // Get the vector object as an array.
  //
  // Parameters:
  //   v
  //     a vec2
  //
  // Return
  //   an array `[x, y]`
  //
  return [v.x, v.y]
}
