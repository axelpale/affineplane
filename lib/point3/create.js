module.exports = (x, y, z) => {
  // affineplane.point3.create(x, y, z)
  //
  // Create a three-dimensional point {x, y, z}
  //
  // Return
  //   a point3
  //
  return {
    x: x,
    y: y,
    z: z
  }
}
